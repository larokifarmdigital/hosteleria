/**
 * Color accent de Ocaña (mismo hex que el `--color-accent` del global.css).
 * Se usa para:
 *   · <meta name="theme-color"> — colorea la barra superior del navegador móvil.
 *   · theme_color del PWA manifest — splash screen y barra al instalar como app.
 *
 * Dorado envejecido (Ocaña, salón bajo bóvedas de ladrillo en Plaça Reial).
 */
export const accentColor = '#c9a961';
